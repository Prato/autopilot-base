#!/bin/env bash
# set -e

# something like:
# version {
      # echo "update.sh version called. "

#     local new_ver = shift
#     sed -i -e '/^Version.*$/Version ${NEW_VER}/' README.md
#     git tag $NEW_VER
#     git push --follow-tags
# }
# except auto-gen the ver tag in here.
# cmd=$1
# if [ ! -z "$cmd" ]; then
#     shift 1
#     $cmd "$@"
#     exit
# fi
#
# help
