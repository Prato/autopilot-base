export RELEASE="test-0"
export PACKAGES="alpine-baselayout,apk-tools,alpine-keys,libc-utils"
export BUILD_OPTIONS=(-b -s -t UTC -r $RELEASE -m $MIRROR -p $PACKAGES)
export TAGS=(autopilot-base:test-0)
